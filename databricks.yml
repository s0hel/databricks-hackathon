bundle:
  name: hackathon-health

variables:
  postgres_branch:
    description: Full Lakebase Postgres branch resource name. Obtain by running `databricks postgres list-branches {project-name}`, select the desired item from the output array and use its .name value. Requires the project resource name.
  postgres_database:
    description: Full Lakebase Postgres database resource name. Obtain by running `databricks postgres list-databases {branch-name}`, select the desired item from the output array and use its .name value. Requires the branch resource name.
  postgres_project:
    description: Full Lakebase Postgres project resource name. Obtain by running `databricks postgres list-projects`, select the desired item from the output array and use its .name value.
  genie_space_id:
    description: Genie Space ID for natural language data questions.
  genie_space_name:
    description: Genie Space display name.

resources:
  apps:
    app:
      name: "hackathon-health"
      description: "Hackathon health indicators app backed by Lakebase synced tables"
      source_code_path: ./
      user_api_scopes:
        - dashboards.genie

      # The resources which this app has access to.
      resources:
        - name: postgres
          postgres:
            branch: ${var.postgres_branch}
            database: ${var.postgres_database}
            permission: CAN_CONNECT_AND_CREATE
        - name: genie-space
          genie_space:
            name: ${var.genie_space_name}
            space_id: ${var.genie_space_id}
            permission: CAN_RUN

targets:
  default:
    default: true
    workspace:
      host: https://dbc-5c28588e-8ff0.cloud.databricks.com

    variables:
      postgres_branch: projects/hackathon-lb/branches/production
      postgres_database: projects/hackathon-lb/branches/production/databases/databricks-postgres
      postgres_project: projects/hackathon-lb
      genie_space_id: "01f168d68121184189fe75938078f9d3"
      genie_space_name: "India Health and Facility Data"
